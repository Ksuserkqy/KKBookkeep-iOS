import SwiftUI

struct RecordPage: View {
    var body: some View {
        AppPlaceholderPage(
            titleKey: "tab.record",
            subtitleKey: "record.placeholder",
            systemImage: "plus.circle.fill"
        )
    }
}
